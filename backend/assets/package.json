{
  "repository": {},
  "license": "MIT",
  "scripts": {
    "deploy": "NODE_ENV=production tailwindcss --input=css/app.css --output=../priv/static/assets/app.css --postcss && NODE_ENV=production esbuild js/app.js --bundle --minify --target=es2017 --outdir=../priv/static/assets"
  },
  "dependencies": {
    "phoenix": "file:../deps/phoenix",
    "phoenix_html": "file:../deps/phoenix_html",
    "phoenix_live_view": "file:../deps/phoenix_live_view"
  },
  "devDependencies": {
    "esbuild": "^0.17.11",
    "esbuild-plugin-import-glob": "^0.1.1"
  }
}
