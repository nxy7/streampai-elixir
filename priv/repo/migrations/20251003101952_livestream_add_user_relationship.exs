defmodule Streampai.Repo.Migrations.LivestreamAddUserRelationship do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:livestreams) do
      add :user_id,
          references(:users,
            column: :id,
            name: "livestreams_user_id_fkey",
            type: :uuid,
            prefix: "public"
          ),
          null: false
    end
  end

  def down do
    drop constraint(:livestreams, "livestreams_user_id_fkey")

    alter table(:livestreams) do
      remove :user_id
    end
  end
end
