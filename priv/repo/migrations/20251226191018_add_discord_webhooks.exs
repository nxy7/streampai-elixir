defmodule Streampai.Repo.Migrations.AddDiscordWebhooks do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:discord_webhooks, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :name, :text, null: false
      add :webhook_url, :text, null: false
      add :event_types, {:array, :text}, null: false
      add :is_enabled, :boolean, null: false, default: true
      add :include_amount, :boolean, null: false, default: true
      add :include_message, :boolean, null: false, default: true
      add :custom_template, :map
      add :last_tested_at, :utc_datetime_usec
      add :last_error, :text
      add :last_error_at, :utc_datetime_usec
      add :successful_deliveries, :bigint, null: false, default: 0
      add :failed_deliveries, :bigint, null: false, default: 0

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :user_id,
          references(:users,
            column: :id,
            name: "discord_webhooks_user_id_fkey",
            type: :uuid,
            prefix: "public"
          ),
          null: false
    end

    create index(:discord_webhooks, [:is_enabled], name: "idx_discord_webhooks_enabled")

    create index(:discord_webhooks, [:user_id], name: "idx_discord_webhooks_user_id")

    create unique_index(:discord_webhooks, [:user_id, :name],
             name: "discord_webhooks_unique_user_webhook_name_index"
           )
  end

  def down do
    drop_if_exists unique_index(:discord_webhooks, [:user_id, :name],
                     name: "discord_webhooks_unique_user_webhook_name_index"
                   )

    drop constraint(:discord_webhooks, "discord_webhooks_user_id_fkey")

    drop_if_exists index(:discord_webhooks, [:user_id], name: "idx_discord_webhooks_user_id")

    drop_if_exists index(:discord_webhooks, [:is_enabled], name: "idx_discord_webhooks_enabled")

    drop table(:discord_webhooks)
  end
end
